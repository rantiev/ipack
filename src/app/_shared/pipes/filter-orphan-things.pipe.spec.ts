import { FilterOrphanThingsPipe } from './filter-orphan-things.pipe'

describe('FilterOrphanThingsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOrphanThingsPipe()
    expect(pipe).toBeTruthy()
  })
})
